{
  "name": "steel-fault-classifier",
  "private": true,
  "scripts": {
    "install:frontend": "npm --prefix frontend ci",
    "build": "npm run install:frontend && npm --prefix frontend run build"
  }
}
